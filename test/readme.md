# Tests

The tests in this folder are written in Hurl (see [docs](https://hurl.dev/docs/manual.html)).

Install Hurl with instructions as per the documentation.

Configure the JMX HTTP Bridge Plugin:

* Enable JMX
* Set authentication to shared secret
* Set a secret value, and add the value in test.env
